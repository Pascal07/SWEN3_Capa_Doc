info:
  name: Resolve Link Not Found
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/links/does-not-exist?password=whatever"
  params:
    - name: password
      value: whatever
      type: query

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:8081
  scripts:
    - type: tests
      code: |-
        test("status is 404 Not Found", function () {
            expect(res.getStatus()).to.equal(404);
          });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
